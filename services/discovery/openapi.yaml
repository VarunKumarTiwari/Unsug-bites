openapi: 3.0.3
info:
  title: Discovery Service
  version: 0.1.0
  description: Restaurant catalog + hidden-gem ranking.

paths:
  /restaurants/nearby:
    get:
      summary: List nearby restaurants ordered by hidden-gem score
      description: >
        lat/lng are optional. When both are provided (user granted location),
        results are the hidden-gem-ranked restaurants within radius_m. When
        omitted (user denied or hasn't been asked), the server returns a small
        default set (~5 restaurants) so the home feed is never empty. lat and
        lng must be supplied together — one without the other is a 400.
      parameters:
        - { name: lat, in: query, required: false, schema: { type: number, minimum: -90, maximum: 90 } }
        - { name: lng, in: query, required: false, schema: { type: number, minimum: -180, maximum: 180 } }
        - { name: radius_m, in: query, schema: { type: integer, default: 2000, minimum: 100, maximum: 10000 } }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/RestaurantSummary' }

  /restaurants/{id}:
    get:
      summary: Full restaurant detail
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RestaurantDetail' }

components:
  schemas:
    RestaurantSummary:
      type: object
      required: [id, name, cuisine, neighborhood, hiddenGemScore, heroImage, lat, lng]
      properties:
        id: { type: string }
        name: { type: string }
        cuisine: { type: string }
        neighborhood: { type: string }
        hiddenGemScore: { type: number, description: '0..1, higher = more underrated' }
        rating: { type: number }
        reviewCount: { type: integer }
        heroImage: { type: string, format: uri }
        lat: { type: number }
        lng: { type: number }
        vibes: { type: array, items: { type: string } }

    RestaurantDetail:
      allOf:
        - $ref: '#/components/schemas/RestaurantSummary'
        - type: object
          properties:
            address: { type: string }
            bestSellers:
              type: array
              items: { $ref: '#/components/schemas/Dish' }
            unsungBites:
              type: array
              items: { $ref: '#/components/schemas/Dish' }

    Dish:
      type: object
      required: [id, name, image]
      properties:
        id: { type: string }
        name: { type: string }
        image: { type: string, format: uri }
        priceCents: { type: integer }
        description: { type: string }
